#include <string.h>
#include <stdio.h>
#include <stdlib.h>


char *MaxNum(int *nums,int numsSize)
{
    assert(nums != NULL && numsSize > 0);
    int i = 0;

}

int main()
{
    int arr[] = {30,31,321,34};
    int len = sizeof(arr)/sizeof(arr[0]);

    return 0;
}
